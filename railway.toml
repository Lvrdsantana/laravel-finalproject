[build]
builder = "NIXPACKS"

[deploy]
startCommand = "chmod 777 storage -R && chmod 666 database/database.sqlite && php artisan serve --host=0.0.0.0 --port=$PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

